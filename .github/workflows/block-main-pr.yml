name: Block PRs to main

on:
  pull_request:
    branches:
      - main

jobs:
  block:
    runs-on: ubuntu-latest
    steps:
      - name: Fail PR to main
        run: |
          echo "❌ Pull requests to 'main' are not allowed. Please retarget to 'develop'."
          exit 1

